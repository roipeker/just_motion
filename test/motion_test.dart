void main() {
  /// Can't run tests without flutter_test for Ticker provider.
  /// waiting for some gentle soul to include testing.
  ///
  // test('test ease value', () {
  //   final height = EaseValue(5);
  //   height.ease = 20;
  //   height.target = 40;
  //   height.tick(Duration.zero);
  //
  //   expect(height.value, (40 - 5) / 20);
  // });
}
